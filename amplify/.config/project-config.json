{
  "projectName": "reactscroll",
  "version": "3.1",
  "frontend": "javascript",
  "javascript": {
    "framework": "react",
    "config": {
      "SourceDir": "example/src",
      "DistributionDir": "example/build",
      "BuildCommand": "yarn build && cd example && yarn build",
      "StartCommand": " cd example && yarn start"
    }
  },
  "providers": [
    "awscloudformation"
  ]
}